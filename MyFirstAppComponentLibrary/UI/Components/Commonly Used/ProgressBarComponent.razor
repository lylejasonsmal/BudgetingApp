<div class="progress-container">
    <div class="progress-bar @(IsDanger? "danger" : "")" style="width:@ProgressPercentage%"></div>
</div>

@code {
    [Parameter] public required int Total { get; set; }
    [Parameter] public required int Progress { get; set; }
    [Parameter] public required bool IsDanger { get; set; } = false;

    private int? ProgressPercentage => Total == 0
        ? 0
        : (Progress * 100) / Total;
}
